import styled from 'styled-components'
import { cores } from '../../styles'

export const TagContainer = styled.span`
  background-color: ${cores.verdeMain};
  color: ${cores.branca};
  font-size: 12px;
  font-weight: bold;
  padding: 4px 8px;
  border-radius: 4px;
`
